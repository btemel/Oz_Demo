# Oz Demo

A simple Python (FastAPI) project for testing Oz cloud agents.

## Setup

```bash
pip install -r requirements.txt
```

## Run

```bash
uvicorn app.main:app --host 0.0.0.0 --port 8000
```

Then visit http://localhost:8000
